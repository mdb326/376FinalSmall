Have to be on varda to use /scratch directory

ssh mdb326@varda.cse.lehigh.edu

cd cse376/376Final

module load gcc-15.2.0


encrypted version requires extra message with vector length


Still left to do:
different operations -- evalAdd instead of evalMult
using doubles instead -- CKKS values
multiple clients going to the server
